---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: reclaimpolicies.fallow.dev
spec:
  group: fallow.dev
  names:
    kind: ReclaimPolicy
    listKind: ReclaimPolicyList
    plural: reclaimpolicies
    shortNames:
    - rp
    singular: reclaimpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.idleAfter
      name: Idle After
      type: string
    - jsonPath: .status.enrolledNamespaces
      name: Namespaces
      type: integer
    - jsonPath: .status.observedWorkloads
      name: Workloads
      type: integer
    - jsonPath: .status.reclaimedWorkloads
      name: Reclaimed
      type: integer
    - jsonPath: .spec.dryRun
      name: Dry Run
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ReclaimPolicy declares which idle workloads to reclaim and how
          far to go.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ReclaimPolicySpec defines which workloads a policy governs and how
              aggressively it escalates.
            properties:
              dryRun:
                description: |-
                  DryRun reports every action the policy would take, via events and
                  status, without mutating a single workload.
                type: boolean
              idleAfter:
                description: |-
                  IdleAfter is how long a workload must look idle before Fallow begins
                  escalating it.
                type: string
              namespaceSelector:
                description: |-
                  NamespaceSelector enrolls namespaces. A namespace must match for any
                  workload inside it to be considered. A nil selector enrolls nothing --
                  enrollment is deliberately opt-in, so an empty policy is inert rather
                  than cluster-wide.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              protectedNamespaces:
                description: |-
                  ProtectedNamespaces are never touched even if they match the
                  NamespaceSelector. System namespaces are always protected regardless
                  of what is listed here.
                items:
                  type: string
                type: array
              stages:
                description: Stages is the escalation ladder.
                properties:
                  delete:
                    description: Delete is the third and final stage.
                    properties:
                      after:
                        description: After is the grace period at zero replicas before
                          deletion.
                        type: string
                      archive:
                        default: true
                        description: |-
                          Archive stores the full workload manifest in a ConfigMap before
                          deleting, which is what keeps this stage reversible. Disabling it
                          makes deletion permanent.
                        type: boolean
                    required:
                    - after
                    type: object
                  notify:
                    description: Notify is the first stage. Omitting it disables the
                      policy entirely.
                    properties:
                      message:
                        description: Message is an optional custom line included in
                          the emitted event.
                        type: string
                    type: object
                  scaleToZero:
                    description: ScaleToZero is the second stage.
                    properties:
                      after:
                        description: After is how long a workload stays notified before
                          being scaled down.
                        type: string
                    required:
                    - after
                    type: object
                type: object
              workloadSelector:
                description: |-
                  WorkloadSelector narrows which workloads inside enrolled namespaces
                  are governed. A nil selector matches every workload in those namespaces.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - idleAfter
            - stages
            type: object
          status:
            description: ReclaimPolicyStatus is the observed state of a ReclaimPolicy.
            properties:
              conditions:
                description: Conditions follow the standard Kubernetes condition conventions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              enrolledNamespaces:
                description: EnrolledNamespaces counts namespaces matching the NamespaceSelector.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the spec generation this status
                  reflects.
                format: int64
                type: integer
              observedWorkloads:
                description: ObservedWorkloads counts workloads this policy governs.
                format: int32
                type: integer
              reclaimedWorkloads:
                description: ReclaimedWorkloads counts workloads currently scaled
                  to zero or deleted.
                format: int32
                type: integer
              targets:
                description: Targets is the per-workload breakdown.
                items:
                  description: TargetStatus is the per-workload record of what Fallow
                    has done and why.
                  properties:
                    idleSince:
                      description: |-
                        IdleSince is when the workload was first observed idle. Cleared when
                        it becomes active again.
                      format: date-time
                      type: string
                    kind:
                      description: Kind of the governed workload (currently always
                        Deployment).
                      type: string
                    lastTransitionTime:
                      description: LastTransitionTime is when Stage last changed.
                      format: date-time
                      type: string
                    name:
                      description: Name of the governed workload.
                      type: string
                    namespace:
                      description: Namespace of the governed workload.
                      type: string
                    originalReplicas:
                      description: OriginalReplicas is the replica count captured
                        before scaling to zero.
                      format: int32
                      type: integer
                    reason:
                      description: Reason is a human-readable explanation of the current
                        stage.
                      type: string
                    stage:
                      description: Stage is the rung this workload currently sits
                        on.
                      type: string
                  required:
                  - kind
                  - lastTransitionTime
                  - name
                  - namespace
                  - stage
                  type: object
                type: array
                x-kubernetes-list-type: atomic
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
